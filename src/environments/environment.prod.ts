import { VERSION } from './version';

export const environment = {
  production: true,
  apiHost: '//ascm-api.imaitrox.com:8001',
  hmr: false,
  version: VERSION.tag,
  staticHost: '',
  demoRoutes: []
};
