import { TtclickDirective } from './ttclick.directive';

describe('TtclickDirective', () => {
  it('should create an instance', () => {
    const directive = new TtclickDirective();
    expect(directive).toBeTruthy();
  });
});
